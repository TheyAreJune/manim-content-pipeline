{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "manimuse",
            "type": "shell",
            "command": "$qualityLevel = Read-Host -Prompt 'Specify Quality Modifiers'; cd ${fileDirname}; manim ${fileBasename} $qualityLevel",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}